# Shell scripts must keep LF endings even when checked out on Windows: several
# are COPYed into Linux images, and a CRLF shebang makes the kernel fail to find
# the interpreter ("no such file or directory" on a file that plainly exists).
*.sh text eol=lf
